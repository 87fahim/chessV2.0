name: Deploy Staging

on:
  workflow_dispatch:
  push:
    branches: [staging]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

concurrency:
  group: deploy-staging
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy To Staging Server
    if: ${{ secrets.SERVER_SSH_KEY != '' && secrets.SERVER_HOST != '' && secrets.SERVER_USER != '' && secrets.STAGING_APP_DIR != '' && secrets.STAGING_PM2_APP != '' }}
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Deploy via SSH
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          APP_DIR: ${{ secrets.STAGING_APP_DIR }}
          PM2_APP: ${{ secrets.STAGING_PM2_APP }}
        run: |
          ssh -o StrictHostKeyChecking=no "${SERVER_USER}@${SERVER_HOST}" "
            set -e
            cd '${APP_DIR}'
            git fetch origin
            git checkout staging
            git pull --ff-only origin staging
            test -f .env.staging
            test -f server/.env.staging
            grep -q '^APP_ENV=staging$' server/.env.staging
            npm ci
            npm run build:staging
            cd server
            npm ci
            npm run build
            APP_ENV=staging NODE_ENV=production pm2 restart '${PM2_APP}' --update-env
            sudo nginx -t
            sudo systemctl reload nginx
          "
